using UnityEngine;
using System.Collections;

public class boxplace : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {



		/*float x =0.4f;
		float y =0.3f;
		float xp = (Screen.width * (1 - x)) / 2; 
		float yp = (Screen.height * (1 - y)) / 2;
		Vector3 temp = new Vector3(xp,yp,0);
		this.transform.position = temp;*/
	}
}
